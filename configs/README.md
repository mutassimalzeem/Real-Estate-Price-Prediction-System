# Configs

Reserved for future configuration files such as:

- model parameters
- feature lists
- training settings
- experiment tracking configuration

The current version keeps model parameters inside the notebook to preserve the original workflow.
