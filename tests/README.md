# Tests

Reserved for future tests.

Recommended future tests:

- train and test columns align after encoding
- engineered features exist in both train and test data
- no missing values before model fitting
- prediction output has the correct row count
- submission file contains `Id` and `SalePrice`
